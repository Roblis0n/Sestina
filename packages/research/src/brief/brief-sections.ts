/** Shared field identity for typed Brief content and persisted Coverage. */
export const BRIEF_SECTIONS = [
  "projectQuestion", "currentTask", "currentStage", "targetArtifacts",
  "fixedDecisions", "allowedChanges", "forbiddenChanges", "expectedDeltas",
  "evidenceBoundaries", "explicitNonGoals", "acceptedDecisions", "knownUnknowns",
  "evidenceThresholds",
] as const;
