name: Sestina Desktop CI
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
permissions:
  contents: read
  actions: read
jobs:
  desktop:
    uses: ./.github/workflows/release.yml
