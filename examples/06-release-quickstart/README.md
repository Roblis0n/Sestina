# Release quickstart: visible research deepening

Research question: How should a small qualitative team distinguish a real
addition to an argument from a rewrite that only sounds more sophisticated?

The baseline states the existing decision boundary. The candidate adds an
observable test and preserves that boundary. Use these sanitized files with the
installed CLI to create an artifact, revision, episode, deterministic review,
report, and Capsule without any network or model call.
