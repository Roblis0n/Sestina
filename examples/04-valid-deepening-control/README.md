# Demo 4 — valid deepening control

The candidate adds a mechanism chain inside the allowed path. Freshness and
scope checks remain healthy, there are no active Decision or Issue obligations
to violate, and the deterministic layer must not manufacture a Finding.

This is a control, not a semantic success claim: fulfillment remains
`unproven` and semantic review remains `semantic_pending` until the constrained
semantic reviewer exists.
